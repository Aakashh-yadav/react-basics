import InputBox from "./InputBox";

// while makeing the components in react please make sure make a saperate JS file where you can import all the files and can export all the components at a sigle files so that you don't have import all the components in the app.jsx file .
export {InputBox}